import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import Icon, { iconSizes, iconMapByName } from './index';
import { IconGallery } from './__storie__/IconGallery';

<Meta
  title="Foundation/Icon"
  component={Icon}
  argTypes={{
    size: {
      options: Object.keys(iconSizes),
      control: { type: 'select' }
    },
    name: {
      options: Object.keys(iconMapByName),
      control: { type: 'select' }
    }
  }}
/>

# Icon

Icon component abstract access to all icons that we have

<ArgsTable story="Icon Component" />

<Canvas>
  <Story
    name="Icon Component"
    args={{
      ...Icon.defaultProps,
    }}
  >
    {(args) => <Icon {...args} />}
  </Story>
</Canvas>

## Icons Gallery
<IconGallery>
  {Object.keys(iconMapByName).map((iconName) => (
    <IconGallery.Item
      key={`iconGallery__${iconName}`}
      icon={<Icon name={iconName} size="sm" />}
      description={iconName}
    />
  ))}
</IconGallery>
  