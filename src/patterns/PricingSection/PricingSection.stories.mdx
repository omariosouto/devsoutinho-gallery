import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import PricingSection from './index';

<Meta
  title="Patterns/PricingSection"
  component={PricingSection}
/>

# Pricing Section

Documentation related to "Typography" and use through the text component

<ArgsTable story="PricingSection Component" />

<Canvas>
  <Story
    name="PricingSection Component"
    args={{
      ...PricingSection.defaultProps,
      children: 'sample text'
    }}
  >
    {(args) => (
      <div className="dark">        
        <PricingSection {...args} />
      </div>
    )}
  </Story>
</Canvas>