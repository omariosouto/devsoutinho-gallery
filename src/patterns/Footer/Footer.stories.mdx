import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import Footer from './index';

<Meta
  title="Patterns/Footer"
  component={Footer}
/>

# Pricing Section

Documentation related to "Typography" and use through the text component

<ArgsTable story="Footer Component" />

<Canvas>
  <Story
    name="Footer Component"
    args={{
      ...Footer.defaultProps,
    }}
  >
    {(args) => (
      <Footer {...args} />
    )}
  </Story>
</Canvas>